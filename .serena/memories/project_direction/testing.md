Confirmed user 2026-09-21: use pytest and an easy-to-understand testing/mocking framework. Selected pytest with pytest-mock (mocker fixture), ordinary pytest fixtures/monkeypatch and HTTPX MockTransport for provider I/O. Use a dedicated disposable PostgreSQL test container for migrations, relational ownership, idempotency and state transitions; no SQLite substitute. Mock OpenAI/Composio/browser side effects in automated tests; never use real contacts/applications or silently incur model/provider charges. Preserve readable tests and Rails-style fat model/thin controller + YAGNI/DRY conventions.