Confirmed user conventions for Command Center, 2026-09-21: Rails-inspired fat models, thin controllers in Python/FastAPI. Models own domain validation, state transitions, invariants and audit events; controllers own authentication, request parsing, transaction boundaries and responses. Model methods do not commit. Keep provider/network I/O separate. Follow YAGNI and DRY; prefer beautiful, clear, idiomatic, well-factored code and concrete model methods over speculative generic repository/service layers. These principles complement, never replace, the fat-model architecture. Frontend port is 3001 because 3000 is occupied. Preserve Clerk JWT shared identity, local OpenAI/LangGraph harness, optional Composio and actor-bound extension bridge direction in mem:project_direction/agent_platform. Never store secrets in memory.