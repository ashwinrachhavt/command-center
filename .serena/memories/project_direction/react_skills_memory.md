Confirmed user 2026-09-21: LangGraph should use a ReAct (reason/action/observation) loop with Command Center's FastAPI and Composio tools, plus integrated Skills and Memory. Implement scoped API tool access rather than giving the LLM raw database/shell/browser session access. Version Markdown skills and pin selected skill content/revision to each run. Actor-owned durable memory is user-editable and agent-accessible within explicit tool grants; memory/source text cannot expand permissions or become verified candidate facts automatically. Celery/Redis executes background jobs. Preserve fat models/thin controllers, YAGNI/DRY, pytest + pytest-mock.