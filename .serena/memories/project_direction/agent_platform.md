User-confirmed direction (2026-09-21): Build Command Center as a professional Next.js/shadcn + FastAPI workspace using reference mockups and trycrm.ai. Follow Rails-inspired fat models/thin controllers. PostgreSQL with Alembic now, portable to hosted Supabase PostgreSQL; Clerk remains identity provider. Securely synchronize Clerk session identity between Next.js and FastAPI, with actor-scoped ownership, stable UUIDs, persisted idempotency keys and audit. User will supply Clerk/OpenAI/additional provider keys locally in ignored root .env (Python) and apps/web/.env (Next.js); never store secrets in memory. Add an easy-to-configure local OpenAI/LangGraph agent harness, inspired by Deep Agents, with Composio integrations for a one-person-company operating system. Command Center container orchestrates work; a local browser extension/client talks to the authenticated backend to inspect and fill application forms in the user's existing signed-in browser session. Keep cookies/browser sessions in the browser. Agent-browser can be a host-side execution option; do not mount browser profiles or Docker socket into the server. Preserve the job/career workflow while building reusable agent capabilities. Distinguish future autonomous campaign policies and final submission authority from ordinary coding authorization; no real applications/messages may be submitted merely by implementing the system. User's uncertain AISpot/Youspot/Dharmesh reference is inspiration, not a selected dependency. Keep this direction in the two active specs and HANDOFF, and preserve ongoing frontend/backend implementation.